{
  "//": "Deno config for the CI runtime smoke (deno-smoke.ts). Maps the workspace packages to their built dist so Deno resolves core's bare `@celsian/schema` import. Node's `tsc -b` ignores this file. Requires `pnpm build` first.",
  "imports": {
    "@celsian/core": "../core/dist/index.js",
    "@celsian/schema": "../schema/dist/index.js"
  }
}
